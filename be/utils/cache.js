/**
 * Key: gameId
 * Properties:
 *  server: websocket server
 *  gameData: game state object
 *  players: [uuid1, uuid2]
 *  spectatorCount
 *  ..?
 */
const WSS = Object.create(null);

module.exports = {
  WSS
};